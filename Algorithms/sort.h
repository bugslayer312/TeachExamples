#pragma once

void qsort(int* arr, int count); // recursive
void qsort2(int* arr, int count); // using stack
void generate(int* arr, int count);
void print(int* arr, int count);
bool testIncreasing(int* arr, int count);